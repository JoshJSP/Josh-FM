(()=>{
'use strict';
if(window.MAIRProfileDiscoveryBridge)return;
const $=id=>document.getElementById(id);
function clamp(v){return Math.max(0,Math.min(100,Number(v)||0))}
function source(){return $('discovery')}
function level(v){v=clamp(v);if(v<=15)return'Veilig';if(v<=35)return'Vertrouwd';if(v<=65)return'Gebalanceerd';if(v<=85)return'Avontuurlijk';return'Verrassing'}
function labelText(v){v=clamp(v);return `${v}% · ${level(v)}`}
function currentValue(){const src=source(),stored=Number(localStorage.getItem('jfm_discovery'));return clamp(src?src.value:(Number.isFinite(stored)?stored:50))}
function pushToEngine(value,kind){const src=source();if(!src)return false;src.value=String(clamp(value));src.dispatchEvent(new Event(kind,{bubbles:true}));return true}
function card(){const section=document.createElement('section');section.id='mairProfileDiscoveryCard';section.className='mair-profile-card';const value=currentValue();section.innerHTML=`<div class="mair-profile-section-title"><i>✦</i><span><b>MAIR Discovery</b><small>Bepaal hoe ver MAIR buiten je vertrouwde muzieksmaak zoekt</small></span></div><div style="border:1px solid #292a2f;border-radius:17px;padding:14px;background:#0d0d0f"><div style="display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:12px"><span style="font-size:13px;font-weight:800;color:#f2f2f4">Hoe avontuurlijk?</span><b id="mairProfileDiscoveryValue" style="font-size:13px;color:#ff7a12;white-space:nowrap">${labelText(value)}</b></div><input id="mairProfileDiscovery" type="range" min="0" max="100" step="10" value="${value}" style="width:100%;accent-color:#ff6a00"><div style="display:flex;justify-content:space-between;margin-top:8px;color:#777983;font-size:9px"><span>Veilig</span><span>Verrassing</span></div></div>`;const slider=section.querySelector('#mairProfileDiscovery'),out=section.querySelector('#mairProfileDiscoveryValue');slider.addEventListener('input',()=>{const v=clamp(slider.value);out.textContent=labelText(v);pushToEngine(v,'input')});slider.addEventListener('change',()=>{const v=clamp(slider.value);out.textContent=labelText(v);pushToEngine(v,'change')});return section}
function install(){const page=$('mairProfilePage');if(!page||$('mairProfileDiscoveryCard'))return false;const cards=[...page.querySelectorAll(':scope > .mair-profile-card')],numbers=cards.find(x=>x.querySelector('.mair-profile-section-title b')?.textContent?.trim()==='Jouw cijfers');const node=card();if(numbers)numbers.insertAdjacentElement('afterend',node);else page.prepend(node);return true}
function refresh(){const slider=$('mairProfileDiscovery'),out=$('mairProfileDiscoveryValue');if(!slider||!out)return install();const v=currentValue();if(document.activeElement!==slider)slider.value=String(v);out.textContent=labelText(slider.value);return true}
const events=['jfm:trackchange','mair:taste-feedback','mair:request-confirmed','mair:station-selected','mair:channelchange','mair:mode-analytics','mair:discovery-counted','mair:discoveries-reset','mair:profile-reset','mair:discovery-station-refreshed'];for(const name of events)window.addEventListener(name,()=>setTimeout(refresh,220));
window.addEventListener('pageshow',()=>setTimeout(refresh,150));document.addEventListener('visibilitychange',()=>{if(!document.hidden)setTimeout(refresh,150)});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(refresh,250),{once:true});else setTimeout(refresh,250);setInterval(refresh,2500);
window.MAIRProfileDiscoveryBridge={version:'profile-discovery-bridge-v2-adventure',install,refresh,value:currentValue,level};
})();