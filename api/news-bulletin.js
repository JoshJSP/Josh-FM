const DEFAULT_MODELS=['openai/gpt-oss-120b','openai/gpt-oss-20b'];
const RATE=new Map();
const safe=(v,n=1200)=>String(v??'').replace(/\s+/g,' ').trim().slice(0,n);
async function timedFetch(url,opt={},ms=9000){const c=new AbortController(),timer=setTimeout(()=>c.abort(),ms);try{return await fetch(url,{...opt,signal:c.signal})}finally{clearTimeout(timer)}}
function rateLimit(req,res){const windowMs=60000,limit=8,ip=String(req.headers?.['x-forwarded-for']||req.headers?.['x-real-ip']||'unknown').split(',')[0].trim().slice(0,80),now=Date.now(),fresh=(RATE.get(ip)||[]).filter(at=>now-at<windowMs);if(fresh.length>=limit){const retry=Math.max(1,Math.ceil((windowMs-(now-fresh[0]))/1000));res.setHeader('Retry-After',String(retry));res.status(429).json({error:'rate_limited',detail:`Probeer het over ${retry} seconden opnieuw.`});return false}fresh.push(now);RATE.set(ip,fresh);if(RATE.size>512)for(const[k,hits]of RATE)if(!hits.some(at=>now-at<windowMs))RATE.delete(k);return true}
function item(x={}){const title=safe(x.title,320),summary=safe(x.summary,900),source=safe(x.source||'NOS',60),publishedAt=safe(x.publishedAt,60);return title?{title,summary,source,publishedAt}:null}
const FALLBACK_TRANSITIONS=['Het belangrijkste nieuws','Verder','Ook in het nieuws','Daarnaast','En nog dit','Tot slot'];
function fallback(items,source='NOS Nieuws'){const lines=items.slice(0,6).map((x,i)=>`${FALLBACK_TRANSITIONS[i]||'Verder'}: ${x.title.replace(/[.!?]+$/,'')}.`);return[`Dit is MAIR Nieuws. De berichten komen van ${source}.`,...lines,'Dit was MAIR Nieuws. Je luistert naar MAIR.'].join(' ')}
function varyRepeatedTransitions(text=''){let seen=0;const alternatives=['Verder','Ook in het nieuws','Daarnaast','En nog dit','Tot slot'];return String(text).replace(/\bVerder in het nieuws\b/gi,()=>alternatives[Math.min(seen++,alternatives.length-1)])}
function cleanOutput(text=''){let x=String(text).replace(/```[\s\S]*?```/g,' ').replace(/^\s*[-*#]+\s*/gm,'').replace(/\s+/g,' ').replace(/^['"“”]+|['"“”]+$/g,'').trim();if(!x)return'';x=varyRepeatedTransitions(x);if(!/^Dit is MAIR Nieuws\./i.test(x))x=`Dit is MAIR Nieuws. ${x}`;x=x.replace(/(?:Dit was MAIR Nieuws\.\s*Je luistert naar MAIR\.)+\s*$/i,'').trim();x=`${x} Dit was MAIR Nieuws. Je luistert naar MAIR.`;return x.slice(0,2400).replace(/\s+\S*$/,'').trim()}
function acceptable(text=''){const x=String(text||'');if(x.length<180||x.length>2400)return false;if(/\b(?:AI|API|Groq|Fish Audio|Spotify|prompt|metadata)\b/i.test(x))return false;return/^Dit is MAIR Nieuws\./i.test(x)&&/Dit was MAIR Nieuws\. Je luistert naar MAIR\.?$/i.test(x)}
export default async function handler(req,res){
 if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});
 if(!rateLimit(req,res))return;
 const raw=Array.isArray(req.body?.items)?req.body.items:[],items=raw.map(item).filter(Boolean).slice(0,6),source=safe(req.body?.sourceLabel||'NOS Nieuws',80),time=safe(req.body?.time||'',40);
 if(items.length<3)return res.status(422).json({error:'Te weinig actuele nieuwsitems voor een betrouwbaar bulletin'});
 const deterministic=fallback(items,source),key=process.env.GROQ_API_KEY;
 if(!key)return res.status(200).json({text:deterministic,provider:'deterministic-news',source,items:items.length,fallback:true});
 const explicit=safe(process.env.GROQ_DJ_MODEL||'',120),models=[...new Set([explicit,...DEFAULT_MODELS].filter(Boolean))].slice(0,3),facts=items.map((x,i)=>`${i+1}. TITEL: ${x.title}\nSAMENVATTING: ${x.summary||'(geen extra samenvatting aangeleverd)'}\nBRON: ${x.source}\nGEPUBLICEERD: ${x.publishedAt||'onbekend'}`).join('\n\n');
 const system=`Je bent de neutrale nieuwsredacteur van MAIR. Schrijf een kort Nederlands radiojournaal op basis van uitsluitend de aangeleverde nieuwsitems. Gebruik geen eigen kennis, aannames, gevolgtrekkingen of details die niet letterlijk in TITEL of SAMENVATTING staan. Als een samenvatting ontbreekt, mag je alleen de titel parafraseren zonder extra feiten. Geen meningen, grapjes, sensatie, weersvoorspellingen of politieke duiding. Verander namen, aantallen, locaties en citaten niet. Noem geen techniek, AI of prompts. Geen markdown of regieaanwijzingen. Klink als een rustig professioneel radiojournaal. Gebruik 4 tot 6 onderwerpen, ongeveer 180 tot 240 woorden. Maak de overgangen natuurlijk en gevarieerd: begin niet ieder onderwerp met dezelfde woorden en gebruik “Verder in het nieuws” maximaal één keer in het hele bulletin. Niet elk onderwerp heeft een expliciete overgangszin nodig. Begin exact met “Dit is MAIR Nieuws.” Zeg direct daarna dat de berichten van ${source} komen. Eindig exact met “Dit was MAIR Nieuws. Je luistert naar MAIR.”`;
 const user=`TIJD: ${time}\nBRONLABEL: ${source}\n\nDIT ZIJN DE ENIGE TOEGESTANE FEITEN:\n${facts}\n\nMaak hiervan één vloeiend radiojournaal. Belangrijkste onderwerp eerst. Wissel de formulering tussen onderwerpen af en vermijd herhaalde vaste tussenkopjes. Return uitsluitend de letterlijk uit te spreken tekst.`;
 const attempts=[];
 for(const model of models){try{const r=await timedFetch('https://api.groq.com/openai/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({model,messages:[{role:'system',content:system},{role:'user',content:user}],temperature:.22,top_p:.65,max_completion_tokens:1200,reasoning_effort:'low',include_reasoning:false})},8500),d=await r.json().catch(()=>({}));if(!r.ok){attempts.push({model,status:r.status,error:safe(d?.error?.message||`Groq HTTP ${r.status}`,300)});if([401,403,429].includes(r.status))break;continue}const text=cleanOutput(d?.choices?.[0]?.message?.content||'');if(!acceptable(text)){attempts.push({model,status:502,error:'Nieuwsoutput voldeed niet aan het veilige bulletincontract'});continue}return res.status(200).json({text,provider:'groq-news',model,source,items:items.length,fallback:false,attempts})}catch(e){attempts.push({model,status:e?.name==='AbortError'?504:500,error:e?.name==='AbortError'?'Groq timeout':safe(e?.message||e,300)})}}
 return res.status(200).json({text:deterministic,provider:'deterministic-news',source,items:items.length,fallback:true,attempts});
}
