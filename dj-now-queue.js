// DJ NU: queue a break for the end of the current track. Uses Spotify's live state near the boundary,
// because the app's cached playback.progress_ms does not tick every 200ms on iPhone.
(()=>{
  const btn=document.getElementById('djNow');if(!btn)return;
  let armedForId=null,armedTrack=null,running=false,lastLiveCheck=0;
  const sleep=ms=>new Promise(r=>setTimeout(r,ms));
  const setState=armed=>{btn.dataset.queued=armed?'1':'0';const b=btn.querySelector('b'),s=btn.querySelector('span');if(b)b.textContent=armed?'🎙️ DJ staat klaar':'🎙️ DJ nu';if(s)s.textContent=armed?'Praat na dit nummer':'Laat hem iets vertellen'};
  btn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();if(!playback?.item?.id||djBusy||running)return;if(armedForId===playback.item.id){armedForId=null;armedTrack=null;setState(false);return}armedForId=playback.item.id;armedTrack=trackObj(playback.item);setState(true)},true);
  async function state(){try{return await api('/me/player')}catch{return null}}
  async function resumeNext(outgoingId){await api('/me/player/next',{method:'POST'}).catch(()=>{});await sleep(550);for(let i=0;i<4;i++){await api('/me/player/play',{method:'PUT'}).catch(()=>{});await sleep(650+i*200);const s=await state();if(s?.item?.id&&s.item.id!==outgoingId&&s.is_playing){playback=s;renderPlayback(s);return true}if(i===1&&s?.item?.id===outgoingId){await api('/me/player/next',{method:'POST'}).catch(()=>{});await sleep(450)}}await api('/me/player/play',{method:'PUT'}).catch(()=>{});return false}
  async function run(){if(running||!armedForId||!armedTrack)return;running=true;djBusy=true;const ended=armedTrack,outgoingId=armedForId;armedForId=null;armedTrack=null;setState(false);try{await api('/me/player/pause',{method:'PUT'}).catch(()=>{});const[fact,weather]=await Promise.all([typeof getFact==='function'?getFact(ended):null,typeof getWeather==='function'?getWeather():null]);const text=await makeDJScript(ended,fact,weather,true);const el=document.getElementById('djText');if(el)el.textContent=text;await speakText(text,false);await resumeNext(outgoingId);if(typeof scheduleTalk==='function')scheduleTalk();setTimeout(()=>refresh().catch(()=>{}),500)}catch(e){console.warn('DJ NOW break failed',e);await api('/me/player/play',{method:'PUT'}).catch(()=>{})}finally{djBusy=false;running=false}}
  // Cached UI state gives a cheap early estimate. Once we are near the end, poll Spotify itself.
  setInterval(async()=>{if(running||!armedForId)return;const p=playback;if(!p?.item)return;if(p.item.id!==armedForId){return}const duration=Number(p.item.duration_ms||armedTrack?.duration||0),progress=Number(p.progress_ms||0),remaining=duration-progress;if(duration&&remaining<=8000&&Date.now()-lastLiveCheck>650){lastLiveCheck=Date.now();const live=await state();if(!live?.item||live.item.id!==armedForId)return;playback=live;renderPlayback(live);const liveRemaining=Number(live.item.duration_ms||duration)-Number(live.progress_ms||0);if(live.is_playing&&liveRemaining<=1900)run()}},250);
})();