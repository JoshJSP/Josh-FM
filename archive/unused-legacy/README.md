# Archived unused legacy files

These files were moved out of MAIR's active repository paths after a conservative dependency audit on 2026-08-16.

At the time of archiving they were not loaded by the production HTML/runtime, dynamic MAIR loaders, service-worker CORE cache, or the active predeploy chain. Files with any meaningful uncertainty were deliberately left in place.

This folder is an archive only. Nothing here should be imported or served by the active app without a fresh dependency review.

Git history remains the source of truth for older versions.
