# MAIR NEDERLANDSTALIG strict source

- Primary source: Spotify editorial playlist `Je Moerstaal` (`37i9dQZF1DWUX3x84bv557`).
- Trusted fallback: only a previously cached snapshot from that same playlist.
- Generic Spotify Search is not allowed as a fallback for `MAIR NEDERLANDSTALIG`.
- Station pool cache bumped to `mair_station_pool_cache_v5_nl_editorial_strict` so earlier mixed/search-generated Nederlandstalig pools are ignored.
