// Josh FM PWA platform layer — sole MediaSession owner, iOS metadata reassertion, sleep timer and update UX.
(()=>{
  const $=id=>document.getElementById(id),SLEEP_KEY='jfm_sleep_timer_v1';
  let sleepTimer=null,sleepState=loadSleep(),waitingWorker=null,wasOffline=!navigator.onLine,updateCheckTimer=null,lastMediaTrackId='',mediaTimers=[];
  function ensureStyles(){if(document.querySelector('link[data-jfm-pwa]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href='./pwa-platform.css';l.dataset.jfmPwa='1';document.head.appendChild(l)}
  function loadSleep(){try{return JSON.parse(localStorage.getItem(SLEEP_KEY)||'null')}catch{return null}}
  function saveSleep(x){sleepState=x;try{x?localStorage.setItem(SLEEP_KEY,JSON.stringify(x)):localStorage.removeItem(SLEEP_KEY)}catch{};renderSleep()}
  function normalizeRaw(item){if(!item?.id)return null;try{return trackObj(item)}catch{}return{id:item.id,uri:item.uri||'',name:item.name||'Josh FM',artists:(item.artists||[]).map(a=>a?.name||a).filter(Boolean),album:item.album?.name||'',image:item.album?.images?.[0]?.url||'',duration:Number(item.duration_ms||0),url:item.external_urls?.spotify||''}}
  function currentTrack(){try{const raw=playback?.item||playback?.track_window?.current_track;if(raw?.id){const t=normalizeRaw(raw);if(t)return t}}catch{}try{const s=window.JFMPlaybackState?.get?.();if(s?.trackId){const q=Array.isArray(queue)?queue:[],t=q.find(x=>x.id===s.trackId);if(t)return t}}catch{}return null}
  function mediaArtwork(t){const src=t?.image||'';return src?[{src,sizes:'512x512',type:'image/jpeg'},{src,sizes:'256x256',type:'image/jpeg'},{src,sizes:'96x96',type:'image/jpeg'}]:[]}
  function safePositionState(t,s,forceReset=false){if(typeof navigator.mediaSession?.setPositionState!=='function')return;const durationMs=Number(t?.duration||s?.durationMs||0);if(!Number.isFinite(durationMs)||durationMs<1000)return;let progressMs=forceReset?0:Number(s?.progressMs||0);if(!Number.isFinite(progressMs)||progressMs<0)progressMs=0;if(s?.trackId&&t?.id&&s.trackId!==t.id)progressMs=0;progressMs=Math.min(progressMs,Math.max(0,durationMs-1));const duration=durationMs/1000,position=progressMs/1000;if(position>=duration)return;if(!Number.isFinite(duration)||!Number.isFinite(position)||duration<=0||position<0)return;try{navigator.mediaSession.setPositionState({duration,playbackRate:1,position})}catch{}}
  function updateMediaSession({reset=false}={}){if(!('mediaSession'in navigator))return false;const t=currentTrack();if(!t?.id)return false;const s=window.JFMPlaybackState?.get?.()||{};const changed=t.id!==lastMediaTrackId;lastMediaTrackId=t.id;try{navigator.mediaSession.metadata=new MediaMetadata({title:t.name||'Josh FM',artist:(t.artists||[]).join(', ')||'Josh FM',album:t.album||window.JFMStationClock?.current?.()?.show?.name||'Josh FM',artwork:mediaArtwork(t)});navigator.mediaSession.playbackState=s?.isPlaying?'playing':'paused'}catch{}safePositionState(t,s,reset||changed);return true}
  function reassertMediaSession(reset=false){for(const t of mediaTimers)clearTimeout(t);mediaTimers=[];for(const delay of [0,120,400,1000,2200])mediaTimers.push(setTimeout(()=>updateMediaSession({reset:reset&&delay===0}),delay))}
  function installMediaActions(){if(!('mediaSession'in navigator))return;const bind=(name,fn)=>{try{navigator.mediaSession.setActionHandler(name,fn)}catch{}};bind('play',()=>window.JFMPlayback?.resume?.());bind('pause',()=>window.JFMPlayback?.pause?.());bind('nexttrack',()=>window.JFMPlayback?.next?.());bind('previoustrack',()=>window.JFMPlayback?.previous?.())}
  async function stopPlayback(reason='sleep'){try{await window.JFMPlayback?.pause?.()}catch{}saveSleep(null);setConnectivityNote(reason==='track-end'?'Sleep timer klaar · gestopt na dit nummer.':'Sleep timer klaar · Josh FM is gepauzeerd.')}
  function clearTimer(){if(sleepTimer)clearTimeout(sleepTimer);sleepTimer=null}
  function scheduleSleep(){clearTimer();if(!sleepState)return;if(sleepState.mode==='after-track')return;const left=Number(sleepState.at||0)-Date.now();if(left<=0){stopPlayback('sleep').catch(()=>{});return}sleepTimer=setTimeout(()=>stopPlayback('sleep').catch(()=>{}),Math.min(left,2147483647))}
  function setMinutes(minutes){saveSleep({mode:'time',at:Date.now()+Number(minutes)*60000,minutes:Number(minutes)});scheduleSleep()}
  function stopAfterTrack(){const t=currentTrack();if(!t?.id)return;saveSleep({mode:'after-track',trackId:t.id,setAt:Date.now()})}
  function cancelSleep(){clearTimer();saveSleep(null)}
  function onTrackChange(id){if(sleepState?.mode==='after-track'&&sleepState.trackId&&id&&id!==sleepState.trackId)stopPlayback('track-end').catch(()=>{});updateMediaSession({reset:true});reassertMediaSession(true)}
  function sleepLabel(){if(!sleepState)return'Sleep timer staat uit.';if(sleepState.mode==='after-track')return'Stopt na dit nummer.';const left=Math.max(0,Number(sleepState.at||0)-Date.now()),mins=Math.ceil(left/60000);return`Stopt over ongeveer ${mins} min.`}
  function installSleepCard(){if($('jfmSleepCard'))return;const pane=$('tab-settings');if(!pane)return;const card=document.createElement('article');card.className='card';card.id='jfmSleepCard';card.innerHTML='<div class="kicker">SLEEP TIMER</div><h3>Automatisch stoppen</h3><div class="chips" id="jfmSleepChoices"><button class="chip" data-min="15">15 min</button><button class="chip" data-min="30">30 min</button><button class="chip" data-min="45">45 min</button><button class="chip" data-min="60">60 min</button><button class="chip" data-track="1">Na dit nummer</button></div><p id="jfmSleepStatus" class="muted">Sleep timer staat uit.</p><button id="jfmSleepCancel" class="secondary hidden" type="button">Annuleer sleep timer</button>';const app=$('installHint')?.closest('.card');if(app)pane.insertBefore(card,app);else pane.appendChild(card);$('jfmSleepChoices')?.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.track)stopAfterTrack();else if(b.dataset.min)setMinutes(Number(b.dataset.min))});$('jfmSleepCancel')?.addEventListener('click',cancelSleep);renderSleep()}
  function renderSleep(){const s=$('jfmSleepStatus'),b=$('jfmSleepCancel');if(s)s.textContent=sleepLabel();if(b)b.classList.toggle('hidden',!sleepState)}
  function ensureConnectivityBar(){if($('jfmConnectivity'))return $('jfmConnectivity');const shell=document.querySelector('.shell');if(!shell)return null;const x=document.createElement('div');x.id='jfmConnectivity';x.className='jfm-connectivity hidden';shell.insertBefore(x,shell.firstChild);return x}
  function setConnectivityNote(text='',kind='info'){const x=ensureConnectivityBar();if(!x)return;x.textContent=text;x.dataset.kind=kind;x.classList.toggle('hidden',!text);if(text&&kind==='info')setTimeout(()=>{if(x.textContent===text)x.classList.add('hidden')},4500)}
  function renderNetwork({initial=false}={}){if(!navigator.onLine){wasOffline=true;setConnectivityNote('Geen internet · de app blijft open, maar Spotify en Fish kunnen tijdelijk niet werken.','warn');return}if(wasOffline&&!initial){wasOffline=false;setConnectivityNote('Verbinding hersteld · Josh FM probeert verder te gaan.','info');return}wasOffline=false;if(initial){const x=ensureConnectivityBar();x?.classList.add('hidden')}}
  function showUpdate(reg){if(!reg?.waiting)return;waitingWorker=reg.waiting;let bar=$('jfmUpdateBanner');if(!bar){bar=document.createElement('div');bar.id='jfmUpdateBanner';bar.className='jfm-update-banner';bar.innerHTML='<span>Nieuwe Josh FM-versie klaar.</span><button type="button" id="jfmApplyUpdate">Update</button><button type="button" id="jfmLaterUpdate">Later</button>';document.body.appendChild(bar);$('jfmApplyUpdate')?.addEventListener('click',()=>waitingWorker?.postMessage?.({type:'SKIP_WAITING'}));$('jfmLaterUpdate')?.addEventListener('click',()=>bar.classList.add('hidden'))}bar.classList.remove('hidden')}
  async function checkForUpdate(reg){if(document.visibilityState!=='visible'||!navigator.onLine)return;if(reg?.waiting)showUpdate(reg);try{await reg.update();if(reg.waiting)showUpdate(reg)}catch{}}
  async function installServiceWorkerUX(){if(!('serviceWorker'in navigator))return;try{const reg=await navigator.serviceWorker.ready;if(reg.waiting)showUpdate(reg);reg.addEventListener('updatefound',()=>{const nw=reg.installing;if(!nw)return;nw.addEventListener('statechange',()=>{if(nw.state==='installed'&&navigator.serviceWorker.controller)showUpdate(reg)})});let reloading=false;navigator.serviceWorker.addEventListener('controllerchange',()=>{if(reloading)return;reloading=true;location.reload()});if(updateCheckTimer)clearInterval(updateCheckTimer);updateCheckTimer=setInterval(()=>checkForUpdate(reg),30*60*1000);window.addEventListener('pageshow',()=>setTimeout(()=>checkForUpdate(reg),1200));document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')setTimeout(()=>checkForUpdate(reg),800)})}catch{}}
  function install(){ensureStyles();installSleepCard();installMediaActions();scheduleSleep();renderSleep();renderNetwork({initial:true});updateMediaSession({reset:true});reassertMediaSession(true);installServiceWorkerUX()}
  window.addEventListener('jfm:trackchange',e=>onTrackChange(e.detail?.trackId||''));window.addEventListener('jfm:playback-state',()=>reassertMediaSession(false));window.addEventListener('online',()=>renderNetwork());window.addEventListener('offline',()=>renderNetwork());setInterval(()=>{renderSleep();const s=window.JFMPlaybackState?.get?.();if(s?.isPlaying)updateMediaSession()},2000);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();window.JFMPWA={version:'pwa-v4.1-update-reminder',setSleepMinutes:setMinutes,stopAfterTrack,cancelSleep,get sleep(){return sleepState},updateMediaSession,reassertMediaSession,showUpdate,setConnectivityNote,checkForUpdate};
})();