import fs from 'node:fs';import assert from 'node:assert/strict';
const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');
const files=['mair-dj-memory.js','mair-imaging.js','mair-live-news.js','mair-voice-lab.js','mair-soak-monitor.js','mair-station-director.js','mair-sleep.js','spotify-api-budget.js','spotify-upcoming-truth.js','api/live-headlines.js'];for(const f of files)assert.ok(fs.existsSync(new URL('../'+f,import.meta.url)),`${f} ontbreekt`);
const dj=read('mair-dj-v2.js'),writer=read('api/dj-writer.js'),clock=read('station-clock.js'),rotation=read('rotation-engine.js'),diag=read('mair-diagnostics-hub.js'),sleep=read('mair-sleep.js'),budget=read('spotify-api-budget.js'),upcoming=read('spotify-upcoming-truth.js'),newsRuntime=read('mair-live-news.js'),queue=read('station-queue.js');
assert.match(dj,/v3\.4-deferred-breaks-memory-imaging/);assert.match(dj,/automatic-break-deferred/);assert.match(dj,/MAIRDJMemory\?\.context/);assert.match(dj,/MAIRImaging\?\.beforeBreak/);assert.match(dj,/MAIRDJMemory\?\.record/);assert.ok(dj.indexOf('await pauseMusic(uri)')<dj.indexOf('MAIRImaging?.beforeBreak'),'imaging moet pas na Spotify-hold komen');assert.ok(dj.indexOf('MAIRImaging?.beforeBreak')<dj.indexOf('await playPrepared(pack)'),'imaging moet vóór DJ-audio komen');
assert.match(writer,/TE VERMIJDEN OPENERS/);assert.match(writer,/NIEUWS:/);assert.match(writer,/headline exact te citeren/);assert.match(clock,/MAIR Morning/);assert.match(clock,/MAIR Drive/);assert.match(clock,/MAIR Top 40/);assert.match(clock,/djId:'max'/);assert.match(rotation,/music-director-v2-show-clock/);assert.match(rotation,/targetMomentum/);assert.match(rotation,/musicPattern/);assert.match(rotation,/station==='sleep'\?-\.24/);assert.match(diag,/mairVoiceLabCard/);assert.match(diag,/mairSoakCard/);assert.match(diag,/RADIO BREIN/);
assert.match(sleep,/MAIR SLEEP/);assert.match(sleep,/Stop na dit nummer/);assert.match(sleep,/window\.JFMPlayback\?\.pause/);assert.doesNotMatch(sleep,/api\('\/me\/player/);
assert.match(newsRuntime,/COOLDOWN=27\*60\*1000/);assert.match(newsRuntime,/LAST_SLOT/);assert.match(newsRuntime,/LAST_TITLE/);
assert.match(queue,/if\(id==='hits'\)return 45/);assert.match(queue,/if\(id==='top40'\)return 35/);
assert.match(budget,/POLL_MS=30000/);assert.match(budget,/api-budget-v2-rate-limit-aware/);assert.match(upcoming,/WATCHDOG_MS=15000/);assert.doesNotMatch(upcoming,/setInterval\(\(\)=>sync\(false\),1600\)/);
const news=read('api/live-headlines.js');assert.match(news,/https:\/\/feeds\.nos\.nl\/nosnieuwsalgemeen/);assert.match(news,/source:'NOS'/);
console.log('MAIR full radio product gate: PASS — imaging, memory, shows, Music Director 2.0, half-hour news, Voice Lab, soak, Sleep, repeat lock and rate-limit mitigation wired');
